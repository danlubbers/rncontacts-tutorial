import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  ImageContainer: {
    width: '100%',
    height: 300,
  },

  imageDetail: {
    width: '100%',
    height: 300,
    resizeMode: 'cover',
  },
});
